spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/app_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/El_Salvador}
    username: ${DB_USERNAME:app_user}
    password: ${DB_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
